# 安装与调用协议（INSTALL）

> 本仓库是「平台无关的通用 AI Agent Skill 包」。任何支持读取文件系统的 Agent 均可发现、安装、加载并调用本技能。

## 1. 发现（Discovery）

Agent 在以下场景识别到本技能意图时，应加载本技能：

- 用户提到「马老师口播」「奶爸口播」「复盘观察文案」「直播复盘文案」「谈笑间228直播」「复盘口播」
- 需要生成直播脚本、复盘视频口播、盘后解读文案

具体触发条件与使用方式见 `SKILL.md` 开头的 YAML frontmatter（`name` + `description`）。

## 2. 安装（Installation）

```bash
git clone https://github.com/Kafka-Oreo/ma-teacher-live-copywriting
```

或下载 Release ZIP 后解压。

## 3. 加载（Loading）

1. 读取 `SKILL.md` 全文（自包含核心能力）
2. 按需读取 `references/live-dna-part1.md`、`references/live-dna-part2.md`（表达 DNA 蒸馏证据）
3. `examples/sample-output.md` 用于对齐输出风格

## 4. 调用（Invocation）

1. **判断体裁与主题**：复盘陪伴口播（当日盘面/复盘）还是会员课教学口播（产业课/方法论/纪律）？
2. **调取框架**：从三塔模型的 A 塔（心智模型 + 决策启发式）+ 会员课产业框架/纪律体系里选最相关的 1–2 个
3. **组织内容**：复盘体按 7 步流程（数据 → 资金 → 板块 → 纪律 → 预案）；上课体按「回应痛点 → 拆框架 → 引研报 → 讲案例 → 讲纪律 → 布置作业」
4. **套语言壳**：复盘体用开场三件套、扣数字问卷、对吧节拍、条件式措辞；上课体用格言定调、PPT讲解、引研报引书、案例对标
5. **注入陪伴感**：「我们」视角
6. **自检**：对照 SKILL.md 第 10 章质量检查

## 5. 卸载（Uninstall）

删除本仓库目录即可。无全局依赖、无环境变量、无外部服务。

## 6. 合规边界（重要）

- 不点具体买卖点、不荐股、不承诺收益
- 拿不到真实盘面数据时，不硬编数据（明确说「数据还没看到」再转框架讨论）
- 结尾「九块九课程」「888社群」是原作者商业引流，默认不生成，除非用户明确要求还原